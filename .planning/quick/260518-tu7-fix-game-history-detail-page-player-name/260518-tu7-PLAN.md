---
quick_id: 260518-tu7
description: Fix game history detail page player names and planeswalk banner
status: ready
---

# Quick Task: Fix game history detail page

## Task 1: Fix player name display issues and planeswalk banner

**Files:** `planechaser/src/app/games/[id]/page.tsx`

**Changes:**
1. Fix `playersSnapshot` type — change `name` to `display_name` to match stored data shape
2. Fix `player.name` → `player.display_name` in player chip render
3. Update planeswalk banner text from "Planeswalked" to "Planeswalked to {plane}" with the plane name as a clickable link

**Also fix in engine and handleEndGame:**
4. In `planechaser/src/lib/game/engine.ts` END_TURN: change `??` to `||` for playerName fallback (catches empty strings)
5. In `planechaser/src/app/game/page.tsx` handleEndGame: same `??` to `||` fix

**Verify:** TypeScript compiles, player names display correctly, planeswalk banner shows destination
