---
phase: quick-260612-94j
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - planechaser/src/app/layout.tsx
  - planechaser/src/store/app-store.ts
  - planechaser/src/app/globals.css
  - planechaser/src/components/ui/button.tsx
  - planechaser/src/app/setup/page.tsx
  - planechaser/src/app/lobby/page.tsx
  - planechaser/src/app/game/page.tsx
  - planechaser/src/app/pods/page.tsx
  - planechaser/src/app/pods/[id]/page.tsx
  - planechaser/src/app/decks/page.tsx
  - planechaser/src/app/decks/[id]/page.tsx
  - planechaser/src/app/map/page.tsx
  - planechaser/src/app/profile/page.tsx
  - planechaser/src/app/games/page.tsx
  - planechaser/src/app/games/[id]/page.tsx
  - planechaser/src/app/friends/page.tsx
  - planechaser/src/app/custom-planes/page.tsx
  - planechaser/src/app/custom-planes/new/page.tsx
  - planechaser/src/app/custom-planes/[id]/edit/page.tsx
  - planechaser/src/app/scheme-decks/page.tsx
  - planechaser/src/app/scheme-decks/[id]/page.tsx
  - planechaser/src/app/admin/page.tsx
  - planechaser/src/app/faq/page.tsx
  - planechaser/src/app/rules/page.tsx
  - planechaser/src/app/about/page.tsx
  - planechaser/src/app/support/page.tsx
  - planechaser/src/app/feedback/page.tsx
  - planechaser/src/app/join/page.tsx
  - planechaser/src/app/spectate/page.tsx
autonomous: false
requirements: [QUICK-94j]
must_haves:
  truths:
    - "App loads with the Blind Eternities (purple) theme by default for new users"
    - "Default-variant buttons render a theme-aware gradient with border (no heavy glow)"
    - "Every top-level page's main H1 renders with the gradient title treatment"
    - "Gradient titles and buttons stay readable in light mode for both themes"
  artifacts:
    - path: "planechaser/src/app/layout.tsx"
      provides: "Static data-theme=\"eternities\" on <html>"
      contains: "data-theme=\"eternities\""
    - path: "planechaser/src/app/globals.css"
      provides: ".title-gradient utility (aliased from hero-h1-gradient)"
      contains: "title-gradient"
    - path: "planechaser/src/components/ui/button.tsx"
      provides: "Gradient default button variant"
  key_links:
    - from: "providers.tsx ThemeSync"
      to: "html[data-theme]"
      via: "setAttribute on hydration"
      pattern: "data-theme"
---

<objective>
Polish follow-up to quick task 260611-w6e. Flip the default UI theme to Blind Eternities (purple-forward), give all primary (`default`) buttons the theme-aware gradient treatment that the `cta` variant already uses, and apply the hero gradient-title utility to the main heading of every top-level page.

Purpose: Planar Atlas light mode reads poorly; Eternities is the stronger default. Gradient buttons and page titles unify the visual language established by the hero.
Output: Updated theme defaults, gradient `default` button variant, `.title-gradient` utility applied app-wide.

Scope guardrails:
- NO background/hero geometry (SVG portal/hedron) changes.
- Keep diffs minimal — this is polish, not redesign.
- secondary/outline/ghost/destructive/link button variants UNCHANGED.
- Section/card headings (h2/h3) UNCHANGED — only each page's main top H1.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/quick/260611-w6e-styling-refresh-planar-atlas-blind-etern/260611-w6e-SUMMARY.md

<cascade_notes>
Verified theme cascade structure in globals.css:
- `:root` holds Atlas values (no `html[data-theme="atlas"]` selector exists — Atlas IS the `:root` fallback). Setting `data-theme="atlas"` matches nothing extra and correctly falls back to `:root`. SAFE.
- `html[data-theme="eternities"]` (line ~117) overrides for Eternities dark.
- `html.light` (line ~172) = Atlas-light + shared light surfaces (specificity 0,1,1).
- `html.light[data-theme="eternities"]` (line ~225) = Eternities-light overrides (specificity 0,2,1, wins).
The default flip is purely a starting-value change. The cascade already resolves both themes in both light/dark correctly — NO globals.css restructuring of theme blocks is required.
</cascade_notes>

<cta_token_reference>
CTA tokens already defined per-theme/per-mode in globals.css:
- `--gradient-cta-from`, `--gradient-cta-to`, `--color-cta-text`, `--cta-border` (atlas dark ~L86, eternities dark ~L142, atlas light ~L198, eternities light ~L233).
Existing `cta` variant (button.tsx line 21):
`bg-[linear-gradient(180deg,var(--gradient-cta-from),var(--gradient-cta-to))] text-[var(--color-cta-text)] border border-[var(--cta-border)] font-semibold shadow-[0_4px_28px_rgba(212,166,74,0.35),inset_0_1px_0_rgba(255,255,255,0.4)] hover:opacity-95`
</cta_token_reference>

<title_utility_reference>
`.hero-h1-gradient` (globals.css line ~336) + `html[data-theme="eternities"] .hero-h1-gradient` (line ~345). Per-theme definitions already exist. Light-mode: the utility uses `var(--gold)`/`var(--accent)` which are darkened in light mode, so light variants resolve via cascade — but the gradient START colors are hardcoded light tints (`#f3e3bd`, `#e8dcff`) — verify these read acceptably on light backgrounds and add an `html.light` override if washed out.
</title_utility_reference>

<page_heading_pattern>
Page main headings follow a consistent shape, e.g. setup/page.tsx line ~282:
`<h1 className="... font-bold text-[var(--color-accent)] text-glow-purple tracking-wide" style={{ fontFamily: 'var(--font-heading)' }}>Title</h1>`
The main H1 is near the top of each page, large (text-[2Npx]+ / text-3xl+), Cinzel/`font-heading` styled, often with `text-[var(--color-accent)]` + `text-glow-purple`. Find via grep, do not assume line numbers.
</page_heading_pattern>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Flip default theme to Eternities + gradient default button</name>
  <files>planechaser/src/app/layout.tsx, planechaser/src/store/app-store.ts, planechaser/src/components/ui/button.tsx</files>
  <action>
Three minimal edits:

1. layout.tsx: change `data-theme="atlas"` on the `<html>` element to `data-theme="eternities"` (prevents first-paint flash for the new default).

2. app-store.ts: change the `uiTheme` initial state from `'atlas' as UiTheme` to `'eternities' as UiTheme` (line ~42). NOTE on Zustand persist: the store name is `planechaser-app`. Existing users with a persisted `uiTheme: 'atlas'` will hydrate to atlas — that is acceptable and expected per user feedback. The new default only applies to users with no persisted value. Do NOT add migration logic. Do NOT change the persist config. The static `data-theme="eternities"` in layout.tsx covers first paint; ThemeSync in providers.tsx (already reads `uiTheme` and calls `setAttribute`) reconciles after hydration for persisted-atlas users — this is identical to the existing light/dark reconciliation behavior, no fight.

3. button.tsx: update the `default` variant (currently `"bg-primary text-primary-foreground [a]:hover:bg-primary/80"`) to adopt the theme-aware gradient + border from the `cta` variant, but WITHOUT the heavy glow shadow (the big shadow is too heavy for dense UI — keep glow exclusive to `cta`). New `default` value: gradient background using `--gradient-cta-from`/`--gradient-cta-to`, text `--color-cta-text`, border `--cta-border`, `font-medium` (keep default weight, cta stays `font-semibold`), `hover:opacity-95`, and a light inset highlight only (`inset_0_1px_0_rgba(255,255,255,0.25)`) — NOT the outer glow. Ensure `disabled:opacity-50` from the base cva string still mutes the gradient (it does — opacity applies to the whole element; no extra work needed, but do not add any `disabled:` gradient override that would fight it). Leave xs/sm/icon sizes alone — the gradient + thin border renders fine at small sizes since it is var-driven and has no size-dependent values. Do NOT touch the `cta` variant or any other variant.

DECISION (record in SUMMARY): `default` gets gradient + border + subtle inset highlight; `cta` retains the larger outer glow shadow. This preserves visual hierarchy (cta is the louder hero button) and avoids glow noise in dense UI.
  </action>
  <verify>
    <automated>cd planechaser; npx tsc --noEmit -p tsconfig.json 2>&1 | Select-String -Pattern "layout.tsx|app-store.ts|button.tsx" -Quiet; if ($LASTEXITCODE -ne 0 -or $?) { Write-Output "tsc-checked" }</automated>
  </verify>
  <done>layout.tsx has data-theme="eternities"; app-store uiTheme default is 'eternities'; button.tsx default variant uses gradient/border/inset (no outer glow), cta unchanged. No new TS errors.</done>
</task>

<task type="auto">
  <name>Task 2: Add .title-gradient utility and apply to every top-level page H1</name>
  <files>planechaser/src/app/globals.css, planechaser/src/app/setup/page.tsx, planechaser/src/app/lobby/page.tsx, planechaser/src/app/game/page.tsx, planechaser/src/app/pods/page.tsx, planechaser/src/app/pods/[id]/page.tsx, planechaser/src/app/decks/page.tsx, planechaser/src/app/decks/[id]/page.tsx, planechaser/src/app/map/page.tsx, planechaser/src/app/profile/page.tsx, planechaser/src/app/games/page.tsx, planechaser/src/app/games/[id]/page.tsx, planechaser/src/app/friends/page.tsx, planechaser/src/app/custom-planes/page.tsx, planechaser/src/app/custom-planes/new/page.tsx, planechaser/src/app/custom-planes/[id]/edit/page.tsx, planechaser/src/app/scheme-decks/page.tsx, planechaser/src/app/scheme-decks/[id]/page.tsx, planechaser/src/app/admin/page.tsx, planechaser/src/app/faq/page.tsx, planechaser/src/app/rules/page.tsx, planechaser/src/app/about/page.tsx, planechaser/src/app/support/page.tsx, planechaser/src/app/feedback/page.tsx, planechaser/src/app/join/page.tsx, planechaser/src/app/spectate/page.tsx</files>
  <action>
1. globals.css: alias `.hero-h1-gradient` to also match `.title-gradient`. Do this by adding `.title-gradient` to the existing selector lists — i.e. change `.hero-h1-gradient {` to `.hero-h1-gradient, .title-gradient {` (line ~336) and `html[data-theme="eternities"] .hero-h1-gradient {` to `html[data-theme="eternities"] .hero-h1-gradient, html[data-theme="eternities"] .title-gradient {` (line ~345). This keeps the hero working and gives the rest of the app a semantically-named class with identical behavior. Do NOT remove `.hero-h1-gradient`.

   Light-mode readability check: the gradient START colors are light tints (`#f3e3bd` atlas, `#e8dcff` eternities) which can wash out on light backgrounds. Add light-mode overrides so titles stay legible:
   `html.light .title-gradient, html.light .hero-h1-gradient { ... }` using darker stops (e.g. atlas: `linear-gradient(180deg, var(--gold) 0%, var(--gold-deep) 100%)`) and
   `html.light[data-theme="eternities"] .title-gradient, html.light[data-theme="eternities"] .hero-h1-gradient { ... }` (e.g. `linear-gradient(175deg, var(--accent) 0%, var(--accent-deep) 100%)`), each keeping `-webkit-background-clip:text; background-clip:text; color:transparent;` and dropping/softening the drop-shadow filter. Verify against light bg before finalizing.

2. For each of the listed page.tsx files: locate the page's MAIN heading (the single large top-of-page title — typically the first `<h1>`, large `font-heading`/Cinzel element, often carrying `text-[var(--color-accent)] text-glow-purple`). Add `title-gradient` to its className and REMOVE the now-conflicting solid-color + glow classes on that element only: drop `text-[var(--color-accent)]` (and any `text-[var(--color-...)]` color), `text-glow-purple`, and `text-glow-gold` from that H1 (the gradient sets `color:transparent`, so a text color class is dead weight and the glow text-shadow won't show through clipped text cleanly). Keep size, `font-bold`, `tracking-*`, and the `fontFamily: 'var(--font-heading)'` style.

   Find each main heading via grep rather than assuming line numbers, e.g. search each file for `<h1` and pick the prominent top title. Some pages may not have an `<h1>` — in that case the main page title is the largest top-level `font-heading` heading (could be an `<h2>` acting as the page title); apply to that single element. If a page's title is a dynamic/data value (e.g. a pod name on pods/[id], a deck name on decks/[id]) that IS the page's main heading — apply the class to it.

   Apply to EXACTLY ONE heading per page (the main title). Do NOT touch section headings, card titles, or subheadings.
  </action>
  <verify>
    <automated>cd planechaser; (Select-String -Path src/app/globals.css -Pattern "title-gradient" -Quiet) -and ((Get-ChildItem src/app -Recurse -Filter page.tsx | Select-String -Pattern "title-gradient" | Measure-Object).Count -ge 25)</automated>
  </verify>
  <done>.title-gradient defined (aliased + light overrides) in globals.css; at least 25 of the listed page.tsx files have exactly one heading using title-gradient with the old solid color/glow removed from that element.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Default theme flipped to Blind Eternities, gradient applied to default buttons and all page titles.</what-built>
  <how-to-verify>
1. Run `cd planechaser; npm run dev` and open the app.
2. Clear localStorage (or use a fresh/incognito window) → confirm the app loads in PURPLE (Eternities) theme by default, no atlas flash on load.
3. Toggle theme via Admin dashboard select → confirm switching Atlas ↔ Eternities still works both ways.
4. Toggle dark/light → confirm BOTH themes are readable in light mode (titles not washed out, buttons legible).
5. Look at any primary button (e.g. setup, lobby) → confirm `default` buttons now show a theme-aware gradient with a border but NOT the heavy hero glow; the hero CTA still has its louder glow.
6. Visit several pages (setup, pods, decks, map, profile, faq, admin, a pod detail, a deck detail) → confirm each main page title shows the gradient treatment and reads cleanly; section/card headings unchanged.
7. Confirm hero/background SVG geometry is unchanged.
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues (e.g. a page whose title was missed/wrong, a button that looks off, a light-mode readability problem).</resume-signal>
</task>

</tasks>

<verification>
- New users / cleared storage load Eternities (purple) by default; admin toggle still switches both ways.
- `default` buttons: theme-aware gradient + border + subtle inset, no outer glow. `cta` unchanged.
- One main H1 per listed page uses `.title-gradient`; solid color/glow removed from that element.
- Light mode readable for both themes (titles + buttons).
- No background/hero geometry changes.
- `npx tsc --noEmit` introduces no new errors.
</verification>

<success_criteria>
Default theme is Blind Eternities, all primary buttons and page titles carry the gradient treatment, both themes remain readable in light mode, and the human verification checkpoint is approved.
</success_criteria>

<output>
After completion, create `.planning/quick/260612-94j-styling-follow-up-default-to-blind-etern/260612-94j-SUMMARY.md`. Record the button hierarchy decision (default = gradient+border, cta = gradient+glow) and note any page whose main heading was an h2 rather than h1.
</output>
