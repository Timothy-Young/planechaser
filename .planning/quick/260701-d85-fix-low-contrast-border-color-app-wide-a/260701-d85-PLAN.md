---
phase: quick-260701-d85
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - planechaser/src/app/globals.css
  - planechaser/src/app/(auth)/auth/page.tsx
autonomous: true
requirements: [QUICK-260701-d85]

must_haves:
  truths:
    - "Input field, card, and divider borders are clearly visible (~3:1 contrast) against the background in the default Atlas (dark) theme"
    - "Borders are clearly visible in the Eternities (purple, dark) theme via html[data-theme='eternities']"
    - "Borders are clearly visible in light mode via html.light"
    - "The /auth page 'PlaneChaser' wordmark renders with the same gradient-fill treatment as the home page hero h1"
  artifacts:
    - path: "planechaser/src/app/globals.css"
      provides: "Raised-contrast --border / --input / --border-subtle tokens in each theme block"
      contains: "--border:"
    - path: "planechaser/src/app/(auth)/auth/page.tsx"
      provides: "Auth h1 using the hero-h1-gradient utility class"
      contains: "hero-h1-gradient"
  key_links:
    - from: "planechaser/src/app/(auth)/auth/page.tsx h1"
      to: ".hero-h1-gradient rule in globals.css"
      via: "className"
      pattern: "hero-h1-gradient"
    - from: "border tokens"
      to: "* { @apply border-border } base rule + border-[var(--color-border)] usages"
      via: "CSS custom property --border resolved through @theme --color-border"
      pattern: "--border:"
---

<objective>
Fix two verified design defects app-wide: (1) `--color-border` is too low-contrast (~1.4:1 in Eternities) against the near-black background, making input/card/divider edges nearly invisible; (2) the `/auth` page wordmark uses a flat `text-glow-purple` treatment instead of the gradient utility the home page hero uses.

Purpose: Restore visible UI boundaries (WCAG ~3:1 for component edges) across all three theme variants, and make the auth wordmark visually consistent with the home page.
Output: Adjusted border tokens in `globals.css` (Atlas dark, Eternities dark, light mode) and an auth h1 className that mirrors the home page hero.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md

# The border tokens live in three theme blocks in globals.css (:root Atlas, html[data-theme="eternities"], html.light).
# --border-subtle and --input mirror --border in each block and must move together.
# The .hero-h1-gradient / .title-gradient rule is aliased to the same declaration; the home page hero uses "hero-h1-gradient" with NO text-glow class and NO inline color.
@planechaser/src/app/globals.css
@planechaser/src/app/(auth)/auth/page.tsx
@planechaser/src/app/page.tsx

<interfaces>
Current border tokens per theme block in globals.css (each block also sets a matching --input):

:root (Atlas, dark) — bg #0d0a10
  --border: #3a2f44;         /* ~2:1 — too low */
  --border-subtle: #2a2230;
  --input: #3a2f44;

html[data-theme="eternities"] (dark) — bg #0a0813
  --border: #2b2450;         /* ~1.4:1 — worst offender */
  --border-subtle: #1d1840;
  --input: #2b2450;

html.light — bg #f5f0fa
  --border: #d8d0e4;         /* ~1.5:1 — too low */
  --border-subtle: #e8e2f0;
  --input: #d8d0e4;

Home page hero h1 (planechaser/src/app/page.tsx, ~line 166) — the reference pattern to mirror:
  className="hero-h1-gradient text-[44px] sm:text-[56px] md:text-[68px] font-bold leading-[1.1] tracking-wide"
  style={{ fontFamily: 'var(--font-heading)' }}
  (No text-glow-purple, no color override — the gradient utility handles fill + drop-shadow.)

Auth h1 to change (planechaser/src/app/(auth)/auth/page.tsx, ~line 122):
  className="text-[36px] font-bold text-[var(--color-accent)] text-glow-purple tracking-wider"
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Raise border-token contrast across all three theme blocks</name>
  <files>planechaser/src/app/globals.css</files>
  <action>
    Increase lightness of the border tokens in each of the three theme blocks so `--border` reaches roughly 3:1 WCAG contrast against that block's own `--bg`, keeping edges visible without competing with content. In EACH block update `--border`, its mirrored `--input`, and (proportionally, more conservatively) `--border-subtle`. Do NOT change any other tokens.

    :root (Atlas, dark — bg #0d0a10): set `--border: #544766;` `--input: #544766;` `--border-subtle: #3a3145;`
    html[data-theme="eternities"] (dark — bg #0a0813): set `--border: #4a3f7a;` `--input: #4a3f7a;` `--border-subtle: #2f2758;`
    html.light (bg #f5f0fa): set `--border: #a99bc4;` `--input: #a99bc4;` `--border-subtle: #cfc5df;`

    Leave `html.light[data-theme="eternities"]` alone — it does not define `--border` and correctly inherits the light-mode value. Do not touch the `.glass` hardcoded rgba borders (out of scope). These values were chosen so each border's relative luminance lands near L≈0.06 (dark themes) / L≈0.34 (light) — approximately 3:1 against its own background.
  </action>
  <verify>
    <automated>cd planechaser && grep -v '^[[:space:]]*/\*' src/app/globals.css | grep -cE -- '--border: #(544766|4a3f7a|a99bc4);' | grep -qx 3 && grep -v '^[[:space:]]*/\*' src/app/globals.css | grep -cE -- '--input: #(544766|4a3f7a|a99bc4);' | grep -qx 3 && echo OK</automated>
  </verify>
  <done>All three theme blocks have raised `--border` and `--input` values (#544766 Atlas, #4a3f7a Eternities, #a99bc4 light); `--border-subtle` raised proportionally; no other tokens changed.</done>
</task>

<task type="auto">
  <name>Task 2: Match auth wordmark to the home page hero gradient</name>
  <files>planechaser/src/app/(auth)/auth/page.tsx</files>
  <action>
    Change the auth page `<h1>` (the "PlaneChaser" wordmark, ~line 122) so its treatment mirrors the home page hero h1: use the `hero-h1-gradient` utility class and remove BOTH the flat `text-[var(--color-accent)]` color and the `text-glow-purple` class (the home page hero uses neither — the gradient utility supplies the fill and drop-shadow). Keep the existing size (`text-[36px]`), `font-bold`, `tracking-wider`, and the `style={{ fontFamily: 'var(--font-heading)' }}`. Resulting className: `"hero-h1-gradient text-[36px] font-bold tracking-wider"`. Do not alter the subtitle `<p>` or any other element.
  </action>
  <verify>
    <automated>cd planechaser && grep -qE 'className="hero-h1-gradient text-\[36px\] font-bold tracking-wider"' 'src/app/(auth)/auth/page.tsx' && ! grep -q 'text-glow-purple' 'src/app/(auth)/auth/page.tsx' && echo OK</automated>
  </verify>
  <done>Auth h1 uses `hero-h1-gradient` and no longer references `text-glow-purple` or `text-[var(--color-accent)]`; size/weight/tracking/font preserved.</done>
</task>

</tasks>

<verification>
- `cd planechaser && npx tsc --noEmit` passes (no type regressions from the JSX edit).
- Manual (checkpoint-free, user may spot-check): on /auth in each theme (Atlas dark, Eternities dark, light), the form card and input borders are visibly distinct from the background, and the wordmark shows the gold/purple gradient fill identical to the home page hero.
</verification>

<success_criteria>
- `--border` / `--input` raised in all three theme blocks to ~3:1 against their own backgrounds; `--border-subtle` raised proportionally.
- Auth h1 renders the same gradient wordmark as the home page hero (no flat color, no text-glow).
- No unrelated tokens, components, or styles modified.
</success_criteria>

<output>
After completion, create `.planning/quick/260701-d85-fix-low-contrast-border-color-app-wide-a/260701-d85-SUMMARY.md`
</output>
