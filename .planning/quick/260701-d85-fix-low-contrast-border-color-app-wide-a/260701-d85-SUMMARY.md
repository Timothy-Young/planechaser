---
phase: quick-260701-d85
plan: 01
subsystem: ui
tags: [css, tailwind, design-tokens, contrast, accessibility, theming]

requires:
  - phase: quick-260612-94j
    provides: hero-h1-gradient utility class and Blind Eternities default theme
provides:
  - Raised-contrast --border/--input/--border-subtle tokens in Atlas, Eternities, and light theme blocks (~3:1 WCAG contrast against each theme's background)
  - Auth page wordmark now uses the same hero-h1-gradient treatment as the home page hero h1
affects: [ui, theming, auth]

tech-stack:
  added: []
  patterns:
    - "Border-token contrast fix pattern: raise --border/--input together, --border-subtle more conservatively, in each of the three theme blocks (:root, html[data-theme='eternities'], html.light) without touching other tokens"

key-files:
  created: []
  modified:
    - planechaser/src/app/globals.css
    - "planechaser/src/app/(auth)/auth/page.tsx"

key-decisions:
  - "Plan's verify command for Task 1 assumed --border appears once per theme block (3 total matches); the file actually defines --border twice per block (once in the custom PlaneChaser vars section, once in the shadcn semantic vars section), so the correct total is 6. Adjusted the verify invocation accordingly (Rule 1 - the implementation followed the plan's explicit values in the <action> exactly; only the verification counting logic needed correction to match actual file structure)."
  - "Installed npm dependencies (node_modules was absent in this fresh worktree) to run the plan's required tsc --noEmit check (Rule 3 - blocking issue)."

requirements-completed: [QUICK-260701-d85]

duration: 12min
completed: 2026-07-01
---

# Quick Task 260701-d85: Fix Low-Contrast Border Color App-Wide + Auth Gradient Summary

**Raised --border/--input/--border-subtle token contrast to ~3:1 in all three theme blocks (Atlas, Eternities, light) and switched the /auth page wordmark from a flat accent color + text-glow to the hero-h1-gradient utility used on the home page.**

## Performance

- **Duration:** 12 min
- **Started:** 2026-07-01T16:27:00Z
- **Completed:** 2026-07-01T16:39:24Z
- **Tasks:** 2 completed
- **Files modified:** 2

## Accomplishments
- Input, card, and divider borders are now visibly distinct from the background in all three theme variants (previously as low as ~1.4:1 contrast in Eternities)
- The `/auth` page "PlaneChaser" wordmark now renders with the same gold/purple gradient fill as the home page hero, instead of a flat accent color with a purple text-glow

## Task Commits

1. **Task 1: Raise border-token contrast across all three theme blocks** - `28d7624` (fix)
2. **Task 2: Match auth wordmark to home page hero gradient** - `e1e0242` (fix)

**Plan metadata:** (docs commit handled by orchestrator, not included here)

## Files Created/Modified
- `planechaser/src/app/globals.css` - Raised `--border`/`--input` (both occurrences per theme block) and `--border-subtle` in `:root` (Atlas), `html[data-theme="eternities"]`, and `html.light` blocks
- `planechaser/src/app/(auth)/auth/page.tsx` - Auth `<h1>` className changed from `text-[36px] font-bold text-[var(--color-accent)] text-glow-purple tracking-wider` to `hero-h1-gradient text-[36px] font-bold tracking-wider`

## Decisions Made
- Kept implementation values exactly as specified in the plan's `<action>` blocks (`--border`/`--input`: `#544766` Atlas, `#4a3f7a` Eternities, `#a99bc4` light; `--border-subtle`: `#3a3145`, `#2f2758`, `#cfc5df` respectively).
- Corrected the Task 1 automated `<verify>` match-count from 3 to 6 for `--border` (the token is set twice per theme block — once in the custom PlaneChaser var section, once in the shadcn semantic var section — both occurrences were updated identically per the plan's instruction to update `--border` "in EACH block"). `--input` correctly expects 3 (appears once per block, only in the shadcn section).
- Ran `npm install` in the worktree's `planechaser/` directory since `node_modules` was absent (fresh worktree), required to execute the plan's mandated `tsc --noEmit` verification step.

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 1 - Bug] Corrected Task 1 verify command's expected match count**
- **Found during:** Task 1 verification
- **Issue:** The plan's automated verify command expected exactly 3 matches for `--border: #(544766|4a3f7a|a99bc4);` across the file, but `--border` is defined twice in each of the 3 theme blocks (once in the custom vars section at the top of each block, once in the shadcn semantic vars section lower in the same block) — 6 total occurrences, all correctly updated to the new values per the plan's own instruction to update `--border` (and mirrored `--input`) "in EACH block."
- **Fix:** Re-ran the verify check with the corrected expected count (6 for `--border`, 3 for `--input`, matching actual file structure). No code change was needed — the CSS edits already matched the plan's specified values exactly.
- **Files modified:** None (verification-only correction)
- **Verification:** `grep -cE -- '--border: #(544766|4a3f7a|a99bc4);'` returns 6; `grep -cE -- '--input: #(544766|4a3f7a|a99bc4);'` returns 3; combined check outputs `OK`
- **Committed in:** 28d7624 (Task 1 commit)

**2. [Rule 3 - Blocking] Installed npm dependencies in fresh worktree**
- **Found during:** Final verification (`tsc --noEmit`)
- **Issue:** This worktree had no `node_modules` directory, so `npx tsc` failed with "This is not the tsc command you are looking for" (npx couldn't resolve a local `tsc` binary).
- **Fix:** Ran `npm install` in `planechaser/` to populate `node_modules` (726 packages installed; non-fatal `EPERM` cleanup warnings on a stale `@rolldown` cache subfolder, did not affect install success).
- **Files modified:** None tracked (node_modules is gitignored)
- **Verification:** `npx tsc --noEmit` subsequently ran to completion (see Issues Encountered for its output)
- **Committed in:** N/A (no trackable file changes; environment setup only)

---

**Total deviations:** 2 auto-fixed (1 verify-script correction, 1 blocking environment fix)
**Impact on plan:** No changes to the plan's actual code specification. Both fixes were necessary to correctly validate the work; no scope creep into unrelated code.

## Issues Encountered

- **Worktree base mismatch at startup:** This worktree's HEAD was on an older commit (`f631c70`) than the plan's expected base (`12d76272407d084d605d91e59e25c8434c854e5f`, where the PLAN.md itself was committed). Per the `<worktree_branch_check>` protocol, HEAD was hard-reset to the correct base commit before any edits (safe, since Step 1 confirmed HEAD was on the per-agent branch `worktree-agent-ad32fac65d5fdcd38`, not a protected ref).
- **Pre-existing, out-of-scope TypeScript errors:** `cd planechaser && npx tsc --noEmit` reports 2 errors in `planechaser/tests/multiplayer-session.spec.ts` (lines 14 and 55): test fixture object literals missing `secondPlaneIndex` and `eliminatedPlayerIds` from the `GameState` type. These are unrelated to this task's scope (`globals.css` border tokens and the auth page `h1` className) and were last touched in an unrelated prior commit (`250a547`). Per the deviation rules' scope boundary, these were **not fixed** — logged instead to `.planning/quick/deferred-items.md`. No regressions were introduced in `globals.css` or `page.tsx` by this task's changes; `tsc` reports zero errors in either modified file.
- **Unrelated dev server on port 3000:** A Next.js dev server was found listening on port 3000, but process inspection confirmed it is running from the main repo (`C:\Users\Tim\Desktop\CodeFun\PlaneChaser\planechaser`), not this worktree — left untouched and not used for verification.

## User Setup Required

None - no external service configuration required.

## Next Phase Readiness

- Both fixes are self-contained CSS/JSX changes with no follow-on work required.
- Recommend a quick manual visual check of `/auth` and any page with input fields/cards across Atlas, Eternities, and light mode to confirm the new border contrast reads well (per the plan's manual verification note) — not a blocker, just a nice-to-have before considering this fully closed.
- Pre-existing `multiplayer-session.spec.ts` type errors remain open in `.planning/quick/deferred-items.md` for a future task to address.

## Self-Check: PASSED

- FOUND: planechaser/src/app/globals.css
- FOUND: planechaser/src/app/(auth)/auth/page.tsx
- FOUND: .planning/quick/260701-d85-fix-low-contrast-border-color-app-wide-a/260701-d85-SUMMARY.md
- FOUND: .planning/quick/deferred-items.md
- FOUND commit: 28d7624
- FOUND commit: e1e0242
