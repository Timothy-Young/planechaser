---
phase: quick-260611-w6e
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - planechaser/src/app/globals.css
  - planechaser/src/store/app-store.ts
  - planechaser/src/components/providers.tsx
  - planechaser/src/components/ui/button.tsx
  - planechaser/src/app/admin/page.tsx
  - planechaser/src/app/page.tsx
autonomous: true
requirements: [SKETCH-001-C]

must_haves:
  truths:
    - "App renders in Planar Atlas (gold-forward) theme by default with no flash"
    - "Admin can switch to Blind Eternities (purple-forward) theme and it persists across refresh"
    - "Light mode still works (no broken contrast) under both themes — gold is darkened on light backgrounds"
    - "cta button variant renders a 52px theme-aware gradient button used in the home hero"
    - "Home hero shows portal-ring SVG (atlas) / hedron-constellation SVG (eternities) instead of blurred orbs"
    - "H1 in the hero uses the gold gradient (atlas) treatment via a utility class"
    - "h1-h4 inherit Cinzel from the @layer base rule"
  artifacts:
    - path: "planechaser/src/app/globals.css"
      provides: "Two theme variable sets (atlas default + eternities) via html[data-theme], light-mode overrides per theme, cta gradient tokens, .hero-h1-gradient utility, Cinzel base rule"
    - path: "planechaser/src/store/app-store.ts"
      provides: "uiTheme state ('atlas' | 'eternities') with setter, persisted"
    - path: "planechaser/src/components/providers.tsx"
      provides: "data-theme attribute applied to <html> from uiTheme"
    - path: "planechaser/src/components/ui/button.tsx"
      provides: "cta variant + bumped default/lg/icon sizes"
    - path: "planechaser/src/app/page.tsx"
      provides: "SVG hero geometry, gold-ruled badge, gradient H1, cta button"
  key_links:
    - from: "providers.tsx ThemeSync"
      to: "<html data-theme>"
      via: "document.documentElement.setAttribute"
      pattern: "setAttribute\\(['\"]data-theme"
    - from: "admin/page.tsx theme toggle"
      to: "app-store uiTheme"
      via: "useAppStore setUiTheme"
      pattern: "setUiTheme"
    - from: "page.tsx hero CTA"
      to: "button cta variant"
      via: "variant=\"cta\""
      pattern: "variant=[\"']cta[\"']"
---

<objective>
Implement the Sketch 001 winner (Variant C "Planar Atlas") as a two-theme system layered over the existing light/dark mechanism, add a theme-aware `cta` button variant with bumped touch targets, and refresh the home hero with theme-appropriate SVG geometry, a gold-ruled badge, and a gradient H1.

Purpose: Move the app off stock Tailwind violet/amber to a bespoke, MTG-native identity (gold-forward default) while preserving all existing component structure and the existing light/dark toggle.

Output: Updated globals.css token system, Zustand uiTheme state, html[data-theme] application in providers, a `cta` button variant, and a refreshed home hero.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@./CLAUDE.md
@.planning/sketches/001-blind-eternities-palette/index.html
@planechaser/AGENTS.md

<interfaces>
<!-- Current globals.css token contract (planechaser/src/app/globals.css). -->
<!-- Custom tokens live in @theme inline (lines 9-30) referencing literal hex; shadcn semantic tokens are defined in :root (lines 55-77) and html.light (lines 103-121). -->
<!-- @theme inline custom tokens currently hold LITERAL hex values, NOT var() references. To make them theme-switchable via data-theme they must be converted to var() references pointing at vars defined in :root / html[data-theme]. -->

Existing custom tokens (must all be remapped per theme):
  --color-bg, --color-bg-subtle, --color-surface, --color-surface-raised,
  --color-accent, --color-accent-deep, --color-accent-muted,
  --color-gold, --color-gold-muted, --color-cta, --color-cta-hover,
  --color-text, --color-text-secondary, --color-text-muted,
  --color-destructive, --color-border, --color-border-subtle,
  --color-glow-purple, --color-glow-gold, --color-glow-red

Existing shadcn semantic tokens (--background, --foreground, --card, --primary, --ring, etc.) defined as raw vars in :root and html.light.

Zustand store (planechaser/src/store/app-store.ts):
  type Theme = 'dark' | 'light'   // existing light/dark — DO NOT touch
  theme: Theme; toggleTheme()
  // persist name: 'planechaser-app'

Providers (planechaser/src/components/providers.tsx) ThemeSync:
  const theme = useAppStore(s => s.theme)
  useEffect: root.classList.remove('dark','light'); root.classList.add(theme)

Button (planechaser/src/components/ui/button.tsx): cva with variant + size maps.
  Current sizes: default h-8, lg h-9, icon size-8.

Admin header (planechaser/src/app/admin/page.tsx ~line 2249): sticky header with Shield icon + "Admin Dashboard" h1 in a flex row — the theme toggle goes here so it shows across all tabs.

Exact palette + SVG markup: see .vc block (atlas, lines 47-54, 160-189) and .vb block (eternities, lines 38-45, 127-158) and the variant-c / variant-b `<svg>` markup (lines 247-285 hedrons, 317-343 portal) in the sketch index.html.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build the two-theme token system in globals.css</name>
  <files>planechaser/src/app/globals.css</files>
  <action>
Convert the `@theme inline` custom color tokens (lines 9-28: --color-bg through --color-glow-red) from literal hex values to `var()` references (e.g. `--color-bg: var(--bg);`, `--color-accent: var(--accent);`, `--color-gold: var(--gold);`). Define the underlying `--bg`, `--accent`, `--gold`, etc. raw variables in `:root` and theme overrides — this is what lets `data-theme` swap them. Keep `--font-heading`, `--font-body`, `--radius-card`, and the shadcn `--color-*: var(--...)` mappings as-is.

Define theme variable sets so cascade order is: base (atlas/default) in `:root`, then `html[data-theme="eternities"]` override, then `html.light` darkens gold for BOTH themes, then `html.light[data-theme="eternities"]` if a light-eternities-specific tweak is needed.

ATLAS (default — `:root`, gold-forward) raw vars from sketch .vc:
  --bg #0d0a10; --bg-subtle #141017; --surface #1c1622; --surface-raised #261d2e;
  --accent #d4a64a; --accent-deep #9a742c; --accent-muted #9f7aea;
  --gold #d4a64a; --gold-deep #9a742c; --gold-muted #9a742c;
  --purple #9f7aea; --cta #c45c3a; --cta-hover #a84a2d;
  --text #f5f1e8; --text-secondary #c9c2b4; --text-muted #847d6e;
  --border #3a2f44; --border-subtle #2a2230; --destructive #c45c3a;
  --glow-purple rgba(159,122,234,0.3); --glow-gold rgba(212,166,74,0.3); --glow-red rgba(196,92,58,0.3);
  CTA gradient tokens: --gradient-cta-from #e8c476; --gradient-cta-to #c89638; --color-cta-text #1a1208; --cta-border #f0d490.

ETERNITIES (`html[data-theme="eternities"]`, purple-forward) raw vars from sketch .vb:
  --bg #0a0813; --bg-subtle #100d1d; --surface #171331; --surface-raised #1f1a3e;
  --accent #b18aff; --accent-deep #5b2bd6; --accent-muted #cbb8ff;
  --gold #d9a648; --gold-deep #b08030; --gold-muted #b08030;
  --purple #b18aff; --cta #e25c4a; --cta-hover #c94a3a;
  --text #f2f0f7; --text-secondary #b8b3cc; --text-muted #756f8f;
  --border #2b2450; --border-subtle #1d1840; --destructive #e25c4a;
  --glow-purple rgba(91,43,214,0.4); --glow-gold rgba(217,166,72,0.3); --glow-red rgba(226,92,74,0.3);
  CTA gradient tokens (white-text gradient): --gradient-cta-from #4a21b8; --gradient-cta-to #7a44e8; --color-cta-text #ffffff; --cta-border rgba(177,138,255,0.4).

Also map the shadcn semantic tokens per theme in `:root` and `html[data-theme="eternities"]` so --primary/--ring/--accent etc. follow:
  Atlas: --primary #9a742c, --primary-foreground #1a1208, --accent #9a742c, --accent-foreground #1a1208, --ring #d4a64a, --background #0d0a10, --foreground #f5f1e8, --card #1c1622, --popover #1c1622, --secondary #1c1622, --muted #1c1622, --border #3a2f44, --input #3a2f44, --destructive #c45c3a.
  Eternities: --primary #5b2bd6, --primary-foreground #ffffff, --accent #5b2bd6, --accent-foreground #ffffff, --ring #b18aff, --background #0a0813, --foreground #f2f0f7, --card #171331, --popover #171331, --secondary #171331, --muted #171331, --border #2b2450, --input #2b2450, --destructive #e25c4a.

LIGHT MODE (`html.light`): keep the existing light block working, but it currently sets purple values. Restructure so `html.light` provides shared light surfaces/text (bg #f5f0fa region) and DARKENS gold for contrast — set --gold and --gold-deep toward #9a742c / #7a5a20 on light backgrounds. Provide `html.light` (atlas-light: accent → darkened gold #9a742c, primary/ring → #9a742c) and `html.light[data-theme="eternities"]` (accent → #6d28d9, gold darkened to #9a742c). Ensure --color-cta-text and --gradient-cta tokens remain legible on light (atlas CTA gradient stays gold w/ dark text; eternities stays purple w/ white text — both fine on light cards).

Update the hard-coded rgba() literals inside `.glow-*`, `.text-glow-*`, `.card-breathe`, `@keyframes card-breathe`, and `body::before` ONLY if trivial — otherwise leave them (they are harmless purple-tinted glows). Do NOT spend budget rewriting every glow.

Add a hero H1 gradient utility in the main stylesheet body (not @layer base):
  `.hero-h1-gradient { background: linear-gradient(180deg, #f3e3bd 10%, var(--gold) 50%, var(--gold-deep) 95%); -webkit-background-clip: text; background-clip: text; color: transparent; filter: drop-shadow(0 2px 18px rgba(212,166,74,0.3)); }`
  And an eternities override: `html[data-theme="eternities"] .hero-h1-gradient { background: linear-gradient(175deg, #e8dcff 20%, var(--accent) 55%, #7a52e0 95%); -webkit-background-clip: text; background-clip: text; filter: drop-shadow(0 0 24px rgba(140,100,255,0.35)); }`

Add Cinzel base rule inside the existing `@layer base` block:
  `h1, h2, h3, h4 { font-family: var(--font-heading); }`

NOTE: This is Tailwind v4 with `@theme inline`. Theme variable values referenced via `var()` resolve at runtime, so swapping the underlying raw var via data-theme correctly re-themes utilities like `bg-[var(--color-bg)]`. Do NOT move tokens out of @theme inline.
  </action>
  <verify>
    <automated>cd planechaser; npx tsc --noEmit 2>&1 | Select-String -Pattern "globals" -Quiet; if (Select-String -Path src/app/globals.css -Pattern 'data-theme=.eternities' -Quiet) { Write-Output OK }</automated>
  </verify>
  <done>globals.css defines atlas (:root default) + eternities (html[data-theme]) token sets, both custom and shadcn semantic tokens, light-mode gold darkening for both themes, .hero-h1-gradient utility (+eternities override), cta gradient tokens, and h1-h4 Cinzel base rule. @theme inline custom tokens reference var(). App builds.</done>
</task>

<task type="auto">
  <name>Task 2: Add uiTheme to the store, apply data-theme in providers, add cta button variant</name>
  <files>planechaser/src/store/app-store.ts, planechaser/src/components/providers.tsx, planechaser/src/components/ui/button.tsx</files>
  <action>
STORE (app-store.ts): Add a NEW state slice independent of the existing `theme` (light/dark). Add `type UiTheme = 'atlas' | 'eternities'`. Add to AppState: `uiTheme: UiTheme` and `setUiTheme: (t: UiTheme) => void`. Initialize `uiTheme: 'atlas'`. Implement setter with `set({ uiTheme })`. Persist automatically via existing persist middleware (name 'planechaser-app' is unchanged). Do NOT modify the existing `theme`/`toggleTheme`.

PROVIDERS (providers.tsx): In the existing `ThemeSync` component, also read `const uiTheme = useAppStore(s => s.uiTheme)`. In the effect, apply: `document.documentElement.setAttribute('data-theme', uiTheme)`. Add `uiTheme` to the effect dependency array. (Default 'atlas' may also be set as the static `data-theme` attribute on `<html>` in layout.tsx to avoid first-paint flash before hydration — check planechaser/src/app/layout.tsx for the `<html>` tag and add `data-theme="atlas"` there if not present; atlas is the default so a flash to atlas-then-atlas is invisible, and eternities users get one corrected paint which matches the existing light/dark hydration behavior already in the app.)

BUTTON (button.tsx): Add a `cta` variant to the `variant` map:
  `cta: "bg-[linear-gradient(180deg,var(--gradient-cta-from),var(--gradient-cta-to))] text-[var(--color-cta-text)] border-[var(--cta-border)] font-semibold shadow-[0_4px_28px_rgba(212,166,74,0.35),inset_0_1px_0_rgba(255,255,255,0.4)] hover:opacity-95"`
  (Theme-aware: gradient + text color + border all come from per-theme CSS vars defined in Task 1, so atlas renders dark-text-on-gold and eternities renders white-text-on-purple automatically.)
Bump sizes for mobile touch targets: `default` h-8 → h-11; `lg` h-9 → h-12; `icon` size-8 → size-11. Leave xs/sm/icon-xs/icon-sm/icon-lg unchanged. Add a `cta` size if useful, OR rely on default sizing — but the sketch CTA is 52px tall; add the height via className at call site in Task 3 (h-13/52px) rather than a new size, to keep the size map minimal. Keep all existing variant/size entries intact.
  </action>
  <verify>
    <automated>cd planechaser; npx tsc --noEmit 2>&1 | Select-String -Pattern "app-store|providers|button" -Quiet; if ((Select-String -Path src/components/ui/button.tsx -Pattern 'cta:' -Quiet) -and (Select-String -Path src/store/app-store.ts -Pattern 'uiTheme' -Quiet) -and (Select-String -Path src/components/providers.tsx -Pattern 'data-theme' -Quiet)) { Write-Output OK }</automated>
  </verify>
  <done>Store has persisted uiTheme ('atlas' default) + setUiTheme; ThemeSync applies data-theme to html and layout sets data-theme="atlas" for no-flash default; button.tsx has a theme-aware cta variant and bumped default/lg/icon sizes. App typechecks.</done>
</task>

<task type="auto">
  <name>Task 3: Add admin theme toggle + refresh home hero</name>
  <files>planechaser/src/app/admin/page.tsx, planechaser/src/app/page.tsx</files>
  <action>
ADMIN TOGGLE (admin/page.tsx ~line 2249-2258): In the sticky header flex row (after the "Admin Dashboard" h1), add a theme select pinned right (use `ml-auto`). Read `const uiTheme = useAppStore(s => s.uiTheme)` and `const setUiTheme = useAppStore(s => s.setUiTheme)` inside `AdminPage`. Render a styled `<select>` (match the existing admin select styling at ~line 1180-1192: h-8 rounded-lg border border-[var(--color-border)] bg-[var(--color-surface)]/60 text-[11px] text-[var(--color-text)] pl-2 pr-6 appearance-none, font-heading) with options: value="atlas" → "Planar Atlas", value="eternities" → "Blind Eternities". onChange → setUiTheme(e.target.value as 'atlas' | 'eternities'). Optionally add a small Palette/Sparkles lucide icon before it (Sparkles is already imported). Keep it minimal — no new tab, no new section component.

HOME HERO (page.tsx):
1. Ambient background (lines 81-85): Replace the three blurred-orb `<div>`s with an inline `<svg>` that swaps by theme. Read `const uiTheme = useAppStore(s => s.uiTheme)`. Render the PORTAL rings SVG (sketch variant-c markup, lines 317-343: emberGlow radialGradient + concentric circles translate(600 330) + compass points + ember dots) when uiTheme === 'atlas', and the HEDRON constellation SVG (sketch variant-b markup, lines 247-285: voidGlow radialGradient + hedron symbol def + constellation lines + use refs + stars) when uiTheme === 'eternities'. Wrap in the existing `<div className="fixed inset-0 z-0">`; give the svg `className="absolute inset-0 w-full h-full opacity-50"` and `viewBox="0 0 1200 700" preserveAspectRatio="xMidYMid slice"`. Copy SVG markup verbatim from the sketch (convert HTML attrs to JSX: stroke-width→strokeWidth, stop-color→stopColor, stop-opacity→stopOpacity, stroke-dasharray→strokeDasharray, xmlns stays, href stays). Give the two `<defs>` gradient ids unique names to avoid collision if both ever mount (they won't — conditional — but rename eternities ids to voidGlowHero/hedronHero anyway).
2. Badge (lines 95-97): Replace the Sparkles pill with the gold-ruled badge. Remove the `<Sparkles>` usage here. Render: `<div className="inline-flex items-center gap-2 px-5 py-2 border border-[var(--color-gold)]/40 rounded-sm bg-gradient-to-b from-[var(--color-gold)]/8 to-transparent text-[11px] tracking-[0.25em] uppercase text-[var(--color-gold)]" style={{ fontFamily: 'var(--font-heading)' }}>Planechase Companion</div>`.
3. H1 (lines 99-104): Replace the inline `text-[var(--color-accent)] text-glow-purple` color treatment with the gradient utility class. Keep sizing/leading classes; change to `className="hero-h1-gradient text-[44px] sm:text-[56px] md:text-[68px] font-bold leading-[1.1] tracking-wide"` and keep `style={{ fontFamily: 'var(--font-heading)' }}`. Remove text-glow-purple and the accent text color.
4. Hero CTA (lines 112-118): Replace the inline-gradient Button with the new variant: `<Button onClick={...} variant="cta" size="lg" className="h-13 px-8 text-[16px]" style={{ fontFamily: 'var(--font-heading)', height: 52 }}>`. Keep the ChevronRight and label logic. Leave the secondary "Sign In" outline Button as-is (just confirm it still renders fine).

Do NOT restyle the How-It-Works / Features / Stats / bottom-CTA sections — out of scope. The bottom-CTA Button (lines 316-322) MAY optionally be switched to variant="cta" for consistency, but only if trivial; otherwise leave it.

Remove the now-unused `Sparkles` import from page.tsx if no longer referenced.
  </action>
  <verify>
    <automated>cd planechaser; npx tsc --noEmit 2>&1 | Select-String -Pattern "page.tsx|admin" -Quiet; if ((Select-String -Path src/app/page.tsx -Pattern 'hero-h1-gradient' -Quiet) -and (Select-String -Path src/app/page.tsx -Pattern 'variant="cta"' -Quiet) -and (Select-String -Path src/app/admin/page.tsx -Pattern 'setUiTheme' -Quiet)) { Write-Output OK }</automated>
  </verify>
  <done>Admin header has a working Planar Atlas / Blind Eternities select bound to setUiTheme; home hero renders theme-conditional SVG geometry, gold-ruled badge, gradient H1, and the cta-variant button. App typechecks; unused Sparkles import removed if applicable.</done>
</task>

</tasks>

<verification>
- Run `cd planechaser; npx tsc --noEmit` — no new type errors.
- Run `cd planechaser; npm run build` (or `npm run lint`) — compiles clean.
- Manual: load home page → default is gold (atlas), portal-ring SVG, gold gradient H1, gold-ruled badge, gold CTA. Switch theme in /admin → purple (eternities), hedron SVG, purple-gradient H1, purple CTA. Refresh → persists. Toggle light mode (existing profile/game toggle) under each theme → no broken contrast, gold readable.
</verification>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client localStorage → render | uiTheme read from persisted Zustand store and applied to DOM attribute |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-w6e-01 | Tampering | uiTheme value from localStorage | mitigate | setUiTheme typed to 'atlas' \| 'eternities'; data-theme attribute only ever set to those literals; unknown values fall through to atlas default (no selector match = :root atlas) |
| T-w6e-02 | Denial of Service | first-paint flash | accept | Static data-theme="atlas" on <html> covers the default; eternities users see one corrected paint, identical to existing light/dark hydration behavior |
</threat_model>

<success_criteria>
- Planar Atlas (gold) is the default theme with no flash on the default case.
- Admin toggle switches to Blind Eternities (purple) and persists across refresh.
- Both custom (--color-*) and shadcn semantic (--primary/--ring/etc.) tokens re-theme via data-theme.
- Light mode remains functional under both themes with darkened gold for contrast.
- `cta` button variant is theme-aware (dark-text-on-gold atlas / white-text-on-purple eternities) and used in the hero.
- Home hero uses SVG geometry (portal/hedron), gold-ruled badge, gradient H1.
- h1-h4 inherit Cinzel from @layer base.
- No new type/build errors; existing component structure unchanged.
</success_criteria>

<output>
After completion, create `.planning/quick/260611-w6e-styling-refresh-planar-atlas-blind-etern/260611-w6e-SUMMARY.md`
</output>
